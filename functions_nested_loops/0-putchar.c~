#include <stdio.h>
/**
 * main - prints _putchar
 * followed by a new line.
 * Return: (0) to success.
*/

int main(void)
{
  int str[] = {_, p, u, t, c, h, a, r, \0};
int i, j;
j = sizeof(str) / sizeof(int);
 for (i = 0; i < j; i++)
{
putchar(str[i]);
}
putchar('\n');
return (0);
}
