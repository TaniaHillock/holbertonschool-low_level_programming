#include "main.h"
/**
*_isupper - checks for uppercase character
*Return: (1) is an uppercase character, (0) otherwhise
*/

int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1); 
}
else
{
return (0);
}
}
